I want to prevent contradictions as a result of impossible
constraints (e.g. `a > b ∧ b > a`)

This will require formalizing constraints themselves as
propositions...

Though, I don't want to encode an entire SAT solver into Lean.
Even if existence might be easier to prove than optimality.

